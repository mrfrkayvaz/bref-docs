# File Properties

### File Extension
Bref files should use the <span style={{color: 'red'}}>`.bref`</span> extension.
### MIME Type
When transferring Bref files over the internet, use the <span style={{color: 'red'}}>`application/bref`</span> MIME type.